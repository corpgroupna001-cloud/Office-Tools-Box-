{
  "name": "worksuite",
  "version": "1.0.0",
  "private": true,
  "description": "WorkSuite — internal productivity tools (Vercel + Supabase).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "smoke:ui": "node tests/ui-smoke/smoke.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.16",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "puppeteer-core": "^25.10.0"
  }
}
